// Re-exported from @needly/core so the web and mobile runtimes share the
// exact same ScreenComponentProps contract.
export { canDo, labelFor } from "@needly/core";
export type { RuntimeRecord, RuntimeMember, MutationInput, ScreenComponentProps } from "@needly/core";
